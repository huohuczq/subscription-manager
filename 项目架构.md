subscription-manager/
├── scripts/                      # 自动化脚本
│   └── setup-kv.js               # 🪄 一键创建 KV 命名空间并绑定到 wrangler.toml
│
├── src/                          # 核心源码目录
│   ├── index.js                  # 🚀 主入口：Worker Fetch/Cron 定时任务调度总控
│   │
│   ├── api/                      # 🌐 接口控制层 (Controllers)
│   │   ├── router.js             # API 路由分发中心
│   │   ├── admin.js              # 后台页面路由与登录状态鉴权保护
│   │   ├── debug.js              # 环境诊断与调试探针
│   │   ├── utils.js              # 接口通用工具 (Cookie 解析、标签清洗等)
│   │   └── handlers/             # 业务接口处理器
│   │       ├── auth.js           # 登录鉴权与 Cookie 管理
│   │       ├── config.js         # 全局配置管理 (带安全脱敏机制)
│   │       ├── dashboard.js      # 统计面板数据聚合与图表数据计算
│   │       ├── notify.js         # 第三方 Webhook 通知接收网关
│   │       ├── subscriptions.js  # 订阅核心 CRUD 操作及路由分发
│   │       └── test-notification.js # 独立通道测试推送接口
│   │
│   ├── core/                     # 🛠️ 核心基础算法库 (Utils/Core)
│   │   ├── auth.js               # 基于 crypto.subtle 的原生无依赖 JWT 签发与校验
│   │   ├── time.js               # 高精度时区转换、偏移修复与格式化引擎
│   │   ├── lunar.js              # 中国农历/闰月推算核心算法
│   │   └── currency.js           # 多币种与动态实时汇率折算引擎
│   │
│   ├── data/                     # 💾 数据持久层 (Models)
│   │   ├── kv.js                 # Cloudflare KV 读写底层抽象封装
│   │   ├── config.js             # 配置对象读写与默认值填充
│   │   └── subscriptions.js      # 订阅数据读写、越界修复与续订自动推算
│   │
│   ├── services/                 # 🧠 业务服务层 (Services)
│   │   ├── scheduler.js          # 定时任务核心引擎 (到期检查/自动续期/去重防刷)
│   │   └── notify/               # 🚀 消息通知适配器集群 (支持 7 大渠道)
│   │       ├── index.js          # 通知路由分发总机
│   │       ├── reminder.js       # 内容排版引擎 (Emoji/农历/倒计时/金额拼接)
│   │       ├── bark.js           # 🍎 iOS Bark 推送适配器
│   │       ├── email.js          # 📧 Resend 邮件通知适配器
│   │       ├── gotify.js         # 🤖 Gotify 自建推送适配器
│   │       ├── notifyx.js        # ⚡ NotifyX 推送适配器
│   │       ├── telegram.js       # ✈️ Telegram Bot 适配器
│   │       ├── webhook.js        # 🔗 自定义 Webhook 适配器
│   │       └── wechat.js         # 💬 企业微信机器人适配器
│   │
│   └── views/                    # 🖥️ 前端视图层 (SPA 单页架构)
│       ├── pages.js              # 核心路由与模板注入
│       ├── theme-resources.html  # 主题配置与全局 CSS/JS 变量
│       ├── components/           # 🧩 公共组件库
│       │   ├── navbar.html       # 统一响应式导航栏 (带自动高亮逻辑)
│       │   ├── toast.html        # 统一全局提示框组件
│       │   └── lunar-tools.html  # 复杂的双轨(公历+农历)日期选择器核心库
│       └── pages/                # 📄 独立页面模板
│           ├── loginPage.html    # 登录页
│           ├── adminPage.html    # 核心订阅列表管理页 (CRUD 操作)
│           ├── configPage.html   # 系统设置与通知渠道配置页
│           └── dashboardPage.html# 聚合统计数据仪表盘页
│
├── package.json                  # Node.js 依赖配置
├── package-lock.json             # 依赖版本锁定
└── wrangler.toml                 # Cloudflare Workers 部署与 KV 绑定配置